import React, { FC } from "react";
import { IPersonProps } from '../../interfaces/person.dto';
import { Card } from '@material-ui/core';

export const PersonCard: FC<IPersonProps> = (props: IPersonProps) => {
  return (
    <Card>
    </Card>
  );
};
